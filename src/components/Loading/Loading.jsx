import React from 'react';
import { Wrapper, Circle } from './Loading.styled';

const Loading = () => {
    return (
        <Wrapper>
            <Circle />
        </Wrapper>
    );
};

export default Loading;
