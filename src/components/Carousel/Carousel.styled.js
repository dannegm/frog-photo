import styled from 'styled-components';

export const CarouselItemWrapepr = styled.div`
    user-select: none;
    width: 100%;
    padding: 16px;
    display: flex;
    flex: 1;
    flex-direction: column;
    gap: 16px;
`;

export const InfoContainer = styled.div`
    display: flex;
    flex: none;
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
    gap: 16px;
`;
