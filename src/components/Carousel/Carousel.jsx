import PropTypes from 'prop-types';

import {} from './Carousel.styled';

const Carousel = ({ ...props }) => {
    return <>{/* TODO: Content */}</>;
};

Carousel.propTypes = {
    // TODO: Props Definition
};

Carousel.defaultProps = {
    // TODO: Default Props Values
};

export default Carousel;
