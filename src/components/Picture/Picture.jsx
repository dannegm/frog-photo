import PropTypes from 'prop-types';

import {} from './Picture.styled';

const Picture = ({ ...props }) => {
    return <>{/* TODO: Content */}</>;
};

Picture.propTypes = {
    // TODO: Props Definition
};

Picture.defaultProps = {
    // TODO: Default Props Values
};

export default Picture;
